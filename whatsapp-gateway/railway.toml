# Deploy só esta pasta no Railway:
# Service → Settings → Root Directory = whatsapp-gateway

[build]
builder = "nixpacks"

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
