# Settings → Root Directory = whatsapp-gateway
# Settings → Build → Builder = DOCKERFILE (não Railpack/Nixpacks)

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
